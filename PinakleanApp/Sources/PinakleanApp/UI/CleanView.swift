
import SwiftUI

/// The view for reviewing scan results and executing the cleanup process.
struct CleanView: View {
    var body: some View {
        Text("Clean View")
            .font(.largeTitle)
    }
}
