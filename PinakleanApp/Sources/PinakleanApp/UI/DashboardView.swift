
import SwiftUI

/// The main dashboard view, showing an overview and key metrics.
struct DashboardView: View {
    var body: some View {
        Text("Dashboard View")
            .font(.largeTitle)
    }
}
